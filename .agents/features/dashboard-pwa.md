# Feature — Dashboard & PWA

## Purpose
Home cards (month totals / today board) + installable PWA shell.

## Frontend
- `features/dashboard/dashboard.page.{ts,html,scss}` + `dashboard.service.ts`
- `features/settings/settings.page.{ts,html,scss}` + `settings.service.ts`
- Angular service worker / `manifest.webmanifest`

## Notes
Settings may show API base URL from environment.
